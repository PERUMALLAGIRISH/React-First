import React from "react";

function NameList() {
  const names = ['Jon','Doe','Mickle'];
  const nameList = names.map((name, index) => <h3 key={index}>{index} {name}</h3>)
  return (
    <div>
      {nameList}
    </div>
  );
}

export default NameList;
